generator client {
    provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model AttendanceRecord {
  id            String   @id @default(cuid())
  fechaClase    DateTime
  modalidad     String
  carrera       String
  grado         String
  semestre      String
  asignatura    String
  inscritos     Int
  presentes     Int
  ausentes      Int
  carnetausentes String[]
  observaciones String?
  pdfUrl String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@index([createdAt])
}